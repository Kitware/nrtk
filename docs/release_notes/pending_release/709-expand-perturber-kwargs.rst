* Moved ``perturber_kwargs`` from ``PerturberMultivariateFactory`` to ``PerturbImageFactory`` interface.

* Updated ``PerturbImageFactory`` implementations to support ``perturber_kwargs``.

* Updated ``_TestPerturbImageFactory`` to support testing for ``perturber_kwargs``.
