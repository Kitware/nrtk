* ``print_extra_status`` now detects ``nrtk-albumentations``.

* Organized perturbers by type on installation page.

* ``NOPPerturber`` no longer displayed in docs.

* Override module names of ``perturb_image_factory`` classes so public classes are displayed in docs
  instead of private.

* Updated API implementations reference to list individual classes instead of submodules for all
  perturber categories, perturbation factories, and utility functions.

* Updated ``pybsm`` optional dependency pin from ``>=0.14.0`` to ``>=0.14.1``.
