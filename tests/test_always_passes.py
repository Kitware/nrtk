# This is a temporary unit test to force the initial
# pytest CI pipeline to pass instead of fail. This should
# be removed once the first valid unit test is added.
def test_always_passes() -> None:
    assert True
